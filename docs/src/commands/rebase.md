# `gg rebase`

Rebase the current stack onto an updated branch.

```bash
gg rebase [TARGET]
```

- `TARGET` is optional.
- If omitted, `gg` uses the stack base branch.

Example:

```bash
gg rebase
gg rebase main
```
