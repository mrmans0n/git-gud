# `gg reorder`

Reorder commits in the current stack.

```bash
gg reorder [OPTIONS]
```

Options:

- `-o, --order <ORDER>`: comma- or space-separated position/SHA order

Examples:

```bash
gg reorder
gg reorder --order "3,1,2"
```
